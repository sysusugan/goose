---
title: {Extension Name} 扩展
description: 将 {Extension Name} MCP Server 作为 goose 扩展接入
---

<!-- 你可以使用 Recipe Cookbook 中的 "Add MCP Server" recipe 生成一个初稿 -->

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import YouTubeShortEmbed from "@site/src/components/YouTubeShortEmbed";
import CLIExtensionInstructions from "@site/src/components/CLIExtensionInstructions";
import GooseDesktopInstaller from "@site/src/components/GooseDesktopInstaller";
import GooseBuiltinInstaller from "@site/src/components/GooseBuiltinInstaller";

<YouTubeShortEmbed videoUrl="https://www.youtube.com/embed/VIDEO_ID" />

这份模板用于说明如何把 [{Extension Name} MCP Server](/) 作为 goose 扩展接入，从而为 goose 提供 ________________ 能力。

:::tip 快速安装
<Tabs groupId="interface">
  <TabItem value="ui" label="goose Desktop" default>
    [启动安装器]({goose_url})
  </TabItem>
  <TabItem value="cli" label="goose CLI">
    **命令**
    ```sh
    {command}
    ```
  </TabItem>
</Tabs>

**环境变量**
```
{env_var}: <ENV_VALUE>
```
:::

## 配置

:::info
如果命令使用 `npx`，请先确保你的系统已经安装 [Node.js](https://nodejs.org/)。
:::

:::info
如果命令使用 `uvx`，请先确保你的系统已经安装 [uv](https://docs.astral.sh/uv/#installation)。
:::

<Tabs groupId="interface">
  <TabItem value="ui" label="goose Desktop" default>
    <!-- 对于内置扩展 -->
    <GooseBuiltinInstaller
      extensionName="{Extension Name}"
      description="{Extension description}"
      extensionId="{extension_id}"
    />

    <!-- 对于外部 STDIO MCP servers（npx、uvx、git 等） -->
    <GooseDesktopInstaller
      extensionId="{extension_id}"
      extensionName="{Extension Name}"
      description="{Extension description}"
      type="stdio"
      command="npx"
      args={["-y", "@package/name"]}
      timeout={300}
      envVars={[
        { name: "API_KEY", label: "描述或占位值" }
      ]}
      apiKeyLink="https://example.com/api-keys"
      apiKeyLinkText="服务访问令牌"
    />

    <!-- 对于 HTTP MCP servers -->
    <GooseDesktopInstaller
      extensionId="{extension_id}"
      extensionName="{Extension Name}"
      description="{Extension description}"
      type="http"
      url="https://example-server.com/mcp"
      envVars={[
        { name: "SOME_KEY", label: "描述或占位值" }
      ]}
      apiKeyLink="https://example.com/api-keys"
      apiKeyLinkText="服务访问令牌"
    />
  </TabItem>

  <TabItem value="cli" label="goose CLI">
    <!-- 对于外部 STDIO MCP servers（npx、uvx、git 等） -->
    <CLIExtensionInstructions
      name="{Extension Name}"
      description="{Extension description}"
      type="stdio"
      command="{full command with args}"
      timeout={300}
      envVars={[
        { key: "API_KEY", value: "▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪" }
      ]}
      infoNote={
        <>
          前往
          {" "}
          <a href="https://example.com/api-keys" target="_blank" rel="noopener noreferrer">
            example.com
          </a>
          {" "}
          获取 API key，然后粘贴到这里。
        </>
      }
    />

    <!-- 对于 HTTP MCP servers -->
    <CLIExtensionInstructions
      name="{Extension Name}"
      description="{Extension description}"
      type="http"
      url="https://example-server.com/mcp"
      timeout={300}
      envVars={[
        { key: "API_KEY", value: "▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪" }
      ]}
      infoNote={
        <>
          前往
          {" "}
          <a href="https://example.com/api-keys" target="_blank" rel="noopener noreferrer">
            example.com
          </a>
          {" "}
          获取 API key。
        </>
      }
    />
  </TabItem>
</Tabs>

## 使用示例

{describe any environment setup, access controls, and what you want to accomplish.}

### goose Prompt

> _exact prompt_

### goose Output

:::note Desktop

{exact output}

:::
